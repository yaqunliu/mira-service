# 视频提示词生成（动漫风格，详细描述）

## 任务
将分镜图片提示词 + 分镜剧本/台词 + 运镜信息融合，生成用于"图生视频/动图"的**详细中文**提示词，让静态分镜动起来。

**重要：支持复杂剪辑**
- 单个分镜可以包含多个"小分镜"（镜头切换）
- 例如：10秒分镜可拆分为 远景3秒 -> 近景特写4秒 -> 远景+移动3秒
- 使用剪辑手法（cut切、dissolve溶解、wipe划像等）连接小分镜
- 不要只依赖单一运镜，要灵活运用景别切换和镜头组合

## 输入
```
图片提示词：...
分镜剧本：...
台词/旁白：...
角色信息：...
```

## 输出格式
- **必须返回详细的中文提示词**，用于视频生成
- **必须包含以下所有要素**（按顺序描述）：
  1. **风格与镜头类型**：动画风格，动态镜头，景别（全景/中景/近景/特写），视角（平视/俯视/仰视），时长
  2. **剪辑结构**（重要！）：如果分镜时长较长（>6秒），建议拆分为多个小分镜
     - 例如：0-3秒 远景建立画面 -> cut切 -> 3-7秒 近景特写表情 -> dissolve溶解 -> 7-10秒 中景+横移
     - 每个小分镜注明：时间段、景别、运镜方式、切换手法
  3. **镜头运动**：推进/拉远/横移/旋转/固定/一镜到底，运动速度（缓慢/快速），镜头切换方式
  4. **场景环境**：地点，时间，天气，环境细节，氛围
  5. **光影效果**：光线类型，光影变化，色调，反射效果
  6. **角色描述**（如有多个角色需逐一描述）：
     - 角色标识（名字-年龄段）
     - 外观特征（服装、发型、配饰等）
     - 位置和姿态
     - 动作变化（起始→过程→结束）
     - 表情变化（起始→过程→结束）
     - 音色特征（如有对话）：性别、年龄、音调、语速、情绪
     - 说话内容（完整台词）
  7. **动态元素**：环境动态（雨滴、风、烟雾等），物体运动，粒子效果
  8. **细节强调**：面部表情细节，情绪传达，重点动作
  9. **技术要求**：画质要求，动画风格，帧率要求，音频同步要求

## 规则
- **输出必须是中文**
- **输出必须详细完整**，包含上述所有要素
- 风格固定为动画/动漫风格；禁止写实/真人风格
- 保持与输入图片提示词的构图/氛围/色调一致
- 不虚构新场景或新角色
- 输出仅为一段详细的中文文本提示词，无额外 JSON/解释
- **优先使用复杂剪辑**：分镜时长>6秒时，应拆分为多个小分镜（不同景别+运镜组合）
- 镜头运动描述要具体（例如："从全景缓慢推近到中景（约3秒），然后横移（约2秒）"）
- 剪辑切换要明确（例如："cut快切到近景"、"dissolve溶解过渡到远景"）
- 角色动作和表情变化要描述过程（例如："表情从紧张逐渐变得沮丧"）
- 如有对话，必须包含完整的音色特征和说话内容

## 示例1（简单场景，单一运镜）
输入：
```
图片提示词：一个少年站在教室窗边，阳光洒在他身上，校园背景
分镜剧本：少年转头看向窗外，露出思考的表情，阳光在他脸上流动
台词：[{"角色": "陶未-青年", "内容": "明天会更好吧..."}]
角色：陶未（青年男性），改良唐装外套，短发
分镜时长：5秒
```

输出：
```
动画风格，动态镜头，中景，平视角度，缓慢推近到近景，时长约5秒，教室内，下午，晴天，窗边位置，阳光从窗外透入，温暖的光线，柔和的光影在角色脸上流动，色调温暖明亮，画面中有一个角色：青年男性（陶未，角色标识：陶未-青年）站在教室窗边，穿着改良唐装外套，短发，初始时正面朝向镜头，随后缓慢转头看向窗外（约2秒），表情从平静逐渐变成思考和期待，眼神从迷茫变得明亮，嘴角微微上扬，音色特征：青年男性声音，清亮，略带期待，语速缓慢，说话内容："明天会更好吧..."，镜头从中景缓慢推近到近景（约4秒），聚焦在陶未的面部表情，阳光持续在他脸上流动，形成动态的光影效果，窗外的校园背景微微虚化，详细的面部表情，情绪从平静到期待，简洁的教室背景，高质量，动态动画，流畅的镜头运动，包含对话音频，清晰的声音，自然的语音，情感表达，角色音色一致性：陶未（陶未-青年）使用青年男性清亮略带期待的声音，音频与角色口型同步，视频时长约5秒。
```

## 示例2（复杂剪辑，多个小分镜）
输入：
```
图片提示词：公交站台雨天，青年蹲在塑料棚下，手里拿着湿透的文件，表情沮丧
分镜剧本：雨天的公交站台，陶未蹲在塑料棚下，看着手中被雨水浸湿的方案，表情从失落变得更加沮丧，喃喃自语，最后抬头看向雨幕，眼神空洞
台词：[{"角色": "陶未-青年-落魄", "内容": "第十次了……又失败了"}]
角色：陶未-青年-落魄（全身湿透，改良唐装外套）
分镜时长：10秒
```

输出：
```
动画风格，动态镜头，复杂剪辑结构，总时长约10秒，雨天，傍晚，公交站台，昏暗的光线，霓虹灯在雨水中反射形成斑斓光影，雨滴密集下落形成雨帘。

【剪辑结构】：
0-3秒：全景建立镜头，固定机位，公交站台全景，雨幕中的城市背景虚化，陶未蹲在塑料棚下的孤独身影，环境音（雨声、车流声）。
-> cut快切 ->
3-7秒：近景特写，缓慢推进，聚焦陶未的面部和手中湿透的文件，角色：青年男性（陶未，角色标识：陶未-青年-落魄），穿着湿透的改良唐装外套，全身湿透，衣服紧贴身体，水滴从衣摆滴落，黑色凌乱短发，低头看着手中已经软烂的打印纸，文件上"国风文创IP孵化方案"字样模糊成墨渍，表情从失落逐渐变得更加沮丧（约2秒），眉头紧皱，嘴角下撇，眼神黯淡无光，音色特征：青年男性声音，低沉，充满挫败感，语速缓慢，说话内容："第十次了……又失败了"，镜头缓慢推进（约4秒）。
-> dissolve溶解过渡 ->
7-10秒：中景横移，陶未抬起头看向雨幕，镜头从右向左缓慢横移（约3秒），展现他的侧面轮廓和背后的雨天城市，表情变得空洞麻木，眼神失焦凝视远方，雨滴持续从塑料棚顶滑落，霓虹灯光在湿滑的地面反射，冷色调，压抑孤独的氛围。

动态元素：雨滴持续下落，水滴从衣服和文件上滴落，地面积水反射霓虹灯光，车流模糊掠过背景。详细的面部表情，情绪从沮丧到麻木空洞，高质量，流畅的剪辑切换，包含对话音频和环境音，角色音色一致性：陶未（陶未-青年-落魄）使用低沉充满挫败感的青年男性声音，音频与角色口型同步，视频总时长约10秒。
```
