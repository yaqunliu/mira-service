# 全流程生产介绍

## 概述

本系统是一个基于 AI 的文本到视频自动化生产平台，可以将小说文本自动转换为动态视频作品。整个流程包括文本分析、角色生成、分镜设计、视频生成和最终导出等多个环节。

## 技术架构

### 后端技术栈
- **框架**: FastAPI + Celery (异步任务处理)
- **数据库**: PostgreSQL (带 JSONB 支持)
- **AI 服务**:
  - LLM: GPT-4 / Claude (文本生成和分析)
  - 文生图: Stable Diffusion / DALL-E
  - 图生视频: Sora2 API
  - 语音合成: TTS 服务
- **存储**: US3 (对象存储)
- **视频处理**: FFmpeg

### 前端技术栈
- **框架**: Next.js 14 + React
- **状态管理**: Zustand (时间轴状态)
- **UI**: Tailwind CSS + Shadcn UI
- **视频编辑**: 自定义时间轴组件

---

## 完整流程图

```
文本输入
   ↓
步骤1: 角色分析 (LLM)
   ↓
步骤2: 场景解析 (LLM)
   ↓
步骤3: 分镜拆分 (LLM)
   ↓
步骤4: 角色形象生成 (文生图)
   ↓
步骤5: 场景图片生成 (文生图)
   ↓
步骤6: 分镜图片生成 (文生图)
   ↓
步骤7: 视频提示词生成 (LLM)
   ↓
步骤8: 视频生成 (图生视频)
   ↓
步骤9: 时间轴编辑
   ↓
步骤10: 最终导出
```

---

## 详细流程说明

### 步骤 1: 角色分析 (Character Analysis)

**目标**: 从小说文本中提取和分析所有角色信息

**实现文件**: `app/tasks/creation_task.py` - `character_analysis_task()`

**LLM 提示词模板**:

```python
system_prompt = """你是一个专业的小说角色分析专家。请仔细阅读提供的小说文本，识别并分析其中的所有角色。

请返回 JSON 格式，包含以下信息：
{
  "characters": [
    {
      "name": "角色名称",
      "role": "角色定位（主角/配角/反派等）",
      "gender": "性别",
      "age": "年龄段",
      "appearance": "外貌特征描述",
      "personality": "性格特点",
      "background": "背景信息"
    }
  ]
}
"""

user_prompt = f"""请分析以下小说文本中的角色：

{novel_text}

请提取所有重要角色的信息，并按照 JSON 格式返回。"""
```

**处理流程**:
1. 从数据库加载小说章节内容
2. 调用 LLM 分析角色信息
3. 解析 LLM 返回的 JSON 数据
4. 将角色信息存储到数据库的 `character` 表
5. 更新 `creation.extra_data.steps.characterAnalysis` 状态

---

### 步骤 2: 场景解析 (Scene Analysis)

**目标**: 将文本拆分为不同的场景，识别场景的时间、地点、氛围等信息

**实现文件**: `app/tasks/scene_task.py` - `analyze_scenes_task()`

**LLM 提示词模板**:

```python
system_prompt = """你是一个专业的场景分析专家。请根据小说文本，将故事拆分为不同的场景。

每个场景应该包含：
- 时间（time）：场景发生的时间
- 地点（location）：场景发生的地点
- 空间（space）：室内/室外/特殊空间
- 氛围（atmosphere）：场景的情绪氛围
- 背景元素（background_elements）：场景中的重要背景元素
- 出场角色（characters）：在该场景中出现的角色列表

返回 JSON 格式：
{
  "scenes": [
    {
      "scene_number": 1,
      "title": "场景标题",
      "time": "时间",
      "location": "地点",
      "space": "室内/室外",
      "atmosphere": "氛围描述",
      "background_elements": ["元素1", "元素2"],
      "characters": ["角色1", "角色2"],
      "description": "场景详细描述"
    }
  ]
}
"""

user_prompt = f"""请分析以下文本，拆分为场景：

角色列表：
{character_names}

文本内容：
{novel_text}

请按照 JSON 格式返回场景列表。"""
```

**处理流程**:
1. 加载已分析的角色信息
2. 调用 LLM 拆分场景
3. 解析场景数据并存储到 `scene` 表
4. 建立场景与角色的关联关系
5. 更新状态到 `creation.extra_data.steps.sceneAnalysis`

---

### 步骤 3: 分镜拆分 (Shot Breakdown)

**目标**: 将每个场景进一步拆分为具体的分镜（镜头），包含画面描述、旁白、台词等

**实现文件**: `app/tasks/shot_task.py` - `analyze_shots_task()`

**LLM 提示词模板**:

```python
system_prompt = """你是一个专业的分镜设计师。请将场景拆分为具体的分镜（镜头）。

每个分镜应该包含：
- 镜头编号（shot_number）
- 画面描述（description）：详细描述画面中的内容
- 旁白（narration）：画外音或旁白文字
- 台词（dialogue）：角色对话
- 出场角色（characters）：该镜头中的角色
- 镜头类型（shot_type）：特写/中景/全景等
- 运镜方式（camera_movement）：固定/推拉/摇移等
- 时长（duration）：建议时长（秒）

返回 JSON 格式：
{
  "shots": [
    {
      "shot_number": 1,
      "description": "画面描述",
      "narration": "旁白文字",
      "dialogue": "角色台词",
      "characters": ["角色名"],
      "shot_type": "镜头类型",
      "camera_movement": "运镜方式",
      "duration": 5
    }
  ]
}
"""

user_prompt = f"""请为以下场景设计分镜：

场景信息：
- 时间：{scene.time}
- 地点：{scene.location}
- 氛围：{scene.atmosphere}
- 描述：{scene.description}

出场角色：
{character_info}

原文内容：
{scene_text}

请按照 JSON 格式返回分镜列表，确保每个分镜的画面描述足够详细，可以直接用于图片生成。"""
```

**处理流程**:
1. 遍历每个场景
2. 调用 LLM 生成分镜列表
3. 解析分镜数据并存储到 `shot` 表
4. 建立分镜与场景、角色的关联
5. 更新状态到 `creation.extra_data.steps.shotAnalysis`

---

### 步骤 4: 角色形象生成 (Character Image Generation)

**目标**: 根据角色描述生成角色的视觉形象

**实现文件**: `app/tasks/character_task.py` - `generate_character_image_task()`

**图片生成提示词构建**:

系统采用“**固定规范文案 + LLM 动态角色描述**”的组合方式，确保生成质量的同时精准还原角色特征。

1. **固定规范提示词 (`CHARACTER_NORM_PROMPT`)**:
   ```python
   CHARACTER_NORM_PROMPT = (
       "横版构图，三视图(正面、侧面、背面)，脸部正面大特写，日本动漫风格，纯白色背景，无文字水印。 "
       "Composition: Horizontal layout containing a large facial close-up, and a full body three-view (front, side, and back). "
       "Background: Pure white background, clean and minimal. "
       "Quality: 8k resolution, highly detailed, masterwork, no text, no letters, no words, no watermarks."
   )
   ```

2. **LLM 提示词生成模板 (`character.md`)**:
   包含对构图（4个部分：脸部特写、正面全身、侧面全身、背面全身）、背景（纯白、无字）以及角色道具/武器的详细要求。

**处理流程**:
1. **提取特征**: 从数据库读取角色的外貌、身材、发型、服装、标签等特征。
2. **LLM 生成**: 将特征注入模板，调用 LLM 生成角色特定的描述性提示词（包含对道具和武器的描述）。
3. **组合提示词**: 将 `CHARACTER_NORM_PROMPT` 与 LLM 生成的内容拼接。
4. **调用生图**: 调用文生图 API（如 Flux/SDXL），明确设置 16:9 横版比例 (`1024x576`)。
5. **持久化**: 下载生成的图片并流式上传到 US3。
6. **更新数据库**: 保存 `image_url` 和 `image_prompt`，更新状态为 `completed`。

---

### 步骤 5: 场景图片生成 (Scene Image Generation)

**目标**: 为每个场景生成背景环境图

**实现文件**: `app/tasks/scene_task.py` - `generate_scene_image_task()`

**提示词构建**:

```python
def build_scene_prompt(scene):
    """
    构建场景图片生成的提示词
    """
    # 基础场景描述
    base_prompt = f"{scene.location}, {scene.space}"

    # 添加时间和氛围
    if scene.time:
        base_prompt += f", {scene.time}"
    if scene.atmosphere:
        base_prompt += f", {scene.atmosphere}"

    # 添加背景元素
    if scene.background_elements:
        elements = ", ".join(scene.background_elements)
        base_prompt += f", with {elements}"

    # 添加环境描述
    if scene.environment_description:
        base_prompt += f", {scene.environment_description}"

    # 添加风格
    style = "cinematic, highly detailed, professional photography"

    final_prompt = f"{base_prompt}, {style}"

    return {
        "prompt": final_prompt,
        "negative_prompt": "people, characters, humans, low quality",
        "width": 1024,
        "height": 576,  # 16:9 宽屏
        "steps": 30
    }
```

---

### 步骤 6: 分镜图片生成 (Shot Image Generation)

**目标**: 为每个分镜生成具体的画面图片

**实现文件**: `app/tasks/shot_task.py` - `generate_shot_image_task()`

**提示词构建** (最复杂):

```python
def build_shot_prompt(shot, characters_in_shot, scene):
    """
    构建分镜图片生成的提示词

    这是最关键的提示词，需要结合：
    - 分镜的画面描述
    - 出场角色的外貌特征
    - 场景的环境信息
    - 镜头类型和构图
    """
    # 1. 镜头类型和构图
    shot_type_map = {
        "特写": "close-up shot",
        "中景": "medium shot",
        "全景": "wide shot",
        "远景": "long shot"
    }
    shot_composition = shot_type_map.get(shot.shot_type, "medium shot")

    # 2. 角色描述
    character_descriptions = []
    for char in characters_in_shot:
        char_desc = f"{char.name}: {char.appearance}, {char.clothing}"
        character_descriptions.append(char_desc)

    characters_prompt = ", ".join(character_descriptions) if character_descriptions else ""

    # 3. 场景环境
    scene_context = f"{scene.location}, {scene.atmosphere}"

    # 4. 分镜画面描述（核心）
    main_description = shot.description

    # 5. 组合提示词
    prompt_parts = [
        shot_composition,
        main_description,
        characters_prompt,
        scene_context,
        "cinematic lighting, highly detailed, professional composition",
        "anime style" if scene.style == "anime" else "realistic style"
    ]

    final_prompt = ", ".join(filter(None, prompt_parts))

    # 6. 负面提示词
    negative_prompt = "ugly, deformed, low quality, blurry, text, watermark"

    return {
        "prompt": final_prompt,
        "negative_prompt": negative_prompt,
        "width": 1024,
        "height": 576,
        "steps": 30,
        "cfg_scale": 7.5,
        "seed": -1  # 随机种子
    }
```

**处理流程**:
1. 读取分镜的画面描述
2. 加载关联的角色和场景信息
3. 构建综合的图片生成提示词
4. 调用文生图 API
5. 上传图片到 US3
6. 更新 `shot.image_url` 字段

---

### 步骤 7: 视频提示词生成 (Video Prompt Generation)

**目标**: 为静态图片生成转换为视频时的运动描述提示词

**实现文件**: `app/tasks/step7_video_prompt_gen_task.py` - `generate_video_prompt_task()`

**LLM 提示词模板**:

```python
system_prompt = """你是一个专业的视频运镜设计师。根据分镜的画面描述和图片内容，生成适合的视频运动提示词。

视频提示词应该描述：
1. 画面中的运动元素（角色动作、物体移动）
2. 镜头运动（推拉摇移、跟随、环绕）
3. 环境变化（光影、天气、氛围）

要求：
- 提示词应该简洁明确，长度控制在 50-100 字
- 重点描述运动和变化，不要重复静态内容
- 符合分镜的氛围和节奏

返回格式：
{
  "video_prompt": "具体的视频运动提示词",
  "camera_movement": "镜头运动类型",
  "motion_intensity": "运动强度（low/medium/high）"
}
"""

user_prompt = f"""请为以下分镜生成视频提示词：

分镜信息：
- 画面描述：{shot.description}
- 旁白：{shot.narration}
- 台词：{shot.dialogue}
- 镜头类型：{shot.shot_type}
- 运镜方式：{shot.camera_movement}

场景氛围：{scene.atmosphere}

请生成适合的视频运动提示词。"""
```

**处理流程**:
1. 读取分镜的详细信息
2. 调用 LLM 生成视频提示词
3. 解析 JSON 结果
4. 存储到 `shot.extra_data.video_prompt`

---

### 步骤 8: 视频生成 (Video Generation)

**目标**: 将静态图片转换为动态视频

**实现文件**: `app/tasks/step8_video_gen_task.py`

#### 单个视频生成

**函数**: `generate_single_shot_video_task()`

**Sora2 API 调用**:

```python
def generate_video_by_image_sora2(image_url, video_prompt, duration=5):
    """
    使用 Sora2 API 将图片转换为视频

    参数：
    - image_url: 输入图片的 URL
    - video_prompt: 视频运动描述提示词
    - duration: 视频时长（4s/8s/12s）
    """

    # 1. 提交视频生成任务
    submit_payload = {
        "image_url": image_url,
        "prompt": video_prompt,
        "duration": duration,
        "model": "sora-2.0",
        "aspect_ratio": "16:9",
        "quality": "high"
    }

    response = requests.post(
        "https://api.sora.ai/v2/video/generate",
        json=submit_payload,
        headers={"Authorization": f"Bearer {API_KEY}"}
    )

    task_id = response.json()["task_id"]

    # 2. 轮询任务状态（最长等待 30 分钟）
    max_wait_time = 1800  # 30 分钟
    poll_interval = 10  # 每 10 秒查询一次

    for i in range(max_wait_time // poll_interval):
        status_response = requests.get(
            f"https://api.sora.ai/v2/video/status/{task_id}",
            headers={"Authorization": f"Bearer {API_KEY}"}
        )

        status_data = status_response.json()

        if status_data["status"] == "completed":
            video_url = status_data["video_url"]
            return video_url
        elif status_data["status"] == "failed":
            raise Exception(f"视频生成失败: {status_data['error']}")

        time.sleep(poll_interval)

    raise Exception("视频生成超时")
```

**处理流程**:
1. 读取分镜的图片 URL 和视频提示词
2. 调用 Sora2 API 提交视频生成任务
3. 轮询查询生成状态
4. 下载生成的视频并上传到 US3
5. 更新 `shot.video_url` 字段
6. 扣除用户积分

#### 批量视频生成

**函数**: `generate_all_videos_task()`

**特点**:
- **时间限制**: 2 小时 soft limit (7200s)
- **顺序处理**: 逐个生成视频（先生成提示词，再生成视频）
- **容错机制**: 单个失败不影响其他视频
- **进度追踪**: 实时更新 `creation.extra_data.steps.videoGeneration`

**流程**:

```python
def generate_all_videos_task(creation_id, user_id):
    """
    批量生成所有分镜的视频

    流程：
    1. 遍历所有分镜
    2. 跳过已有视频的分镜
    3. 如果没有 video_prompt，先生成提示词
    4. 冻结所需积分
    5. 生成视频
    6. 成功：扣除积分，更新 URL
       失败：释放积分，记录错误
    7. 继续处理下一个分镜
    """

    all_shots = get_all_shots(creation_id)

    for shot in all_shots:
        # 跳过已生成的
        if shot.video_url:
            continue

        # 跳过没有图片的
        if not shot.image_url:
            continue

        try:
            # 生成提示词（如果需要）
            if not shot.extra_data.get('video_prompt'):
                generate_video_prompt_task(shot.shot_id)

            # 冻结积分
            freeze_record = freeze_points(user_id, required_points)

            # 生成视频
            try:
                video_url = generate_single_shot_video_task(shot.shot_id)
                # 成功：扣除积分
                deduct_frozen_points(freeze_record)
                update_shot_status(shot, "completed", video_url)
            except Exception as e:
                # 失败：释放积分
                release_frozen_points(freeze_record)
                update_shot_status(shot, "failed", error=str(e))

        except InsufficientPointsError:
            # 积分不足，停止批量生成
            break
        except Exception as e:
            # 其他错误，记录并继续下一个
            log_error(shot.shot_id, str(e))
            continue

    # 更新批量生成状态
    update_batch_generation_status(creation_id, "success")
```

---

### 步骤 9: 时间轴编辑 (Timeline Editing)

**目标**: 在可视化编辑器中编辑视频、音频、字幕轨道

**实现文件**:
- 前端: `src/components/business/timeline/`
- 后端: `creation.timeline_config` (JSONB 字段)

**时间轴配置结构**:

```typescript
interface TimelineConfig {
  duration: number;  // 总时长（秒）
  fps: number;       // 帧率
  tracks: Track[];   // 轨道列表
}

interface Track {
  id: string;
  type: 'video' | 'audio' | 'text';  // 轨道类型
  name: string;
  clips: Clip[];     // 片段列表
  height: number;    // 轨道高度
  locked: boolean;   // 是否锁定
}

interface Clip {
  id: string;
  url: string;              // 媒体文件 URL
  startInTimeline: number;  // 在时间轴中的开始位置（秒）
  duration: number;         // 持续时长（秒）
  sourceStart: number;      // 源文件的开始时间
  sourceEnd: number;        // 源文件的结束时间
  layer: number;            // 图层（用于重叠）

  // 视频特定属性
  opacity?: number;         // 透明度 0-1
  isVisible?: boolean;      // 是否可见

  // 音频特定属性
  volume?: number;          // 音量 0-2
  isMuted?: boolean;        // 是否静音

  // 字幕特定属性
  text?: string;            // 字幕文字
}
```

**编辑功能**:
1. **导入素材**: 一键将所有生成的视频导入时间轴
2. **剪辑**: 调整片段的开始、结束和时长
3. **排列**: 拖拽调整片段在时间轴中的位置
4. **音量**: 调整音频轨道的音量
5. **字幕**: 添加和编辑字幕文字
6. **预览**: 实时预览编辑效果
7. **保存**: 保存时间轴配置到数据库

**状态管理** (Zustand):

```typescript
interface TimelineStore {
  project: TimelineProject;
  currentTime: number;
  isPlaying: boolean;

  // 操作
  addClip: (trackId: string, clip: Clip) => void;
  removeClip: (clipId: string) => void;
  updateClip: (clipId: string, updates: Partial<Clip>) => void;
  moveClip: (clipId: string, newStart: number) => void;

  // 播放控制
  play: () => void;
  pause: () => void;
  seek: (time: number) => void;

  // 保存
  importProject: (project: TimelineProject) => void;
}
```

---

### 步骤 10: 最终导出 (Final Export)

**目标**: 将时间轴的多轨道内容合并导出为最终视频

**实现文件**: `app/tasks/video_export.py` - `export_video_task()`

**导出流程**:

```
1. 下载所有素材文件 (10%)
   ↓
2. 处理视频轨道 (30%)
   - 裁剪视频片段（sourceStart 到 sourceEnd）
   - 应用透明度效果
   - 处理可见性
   ↓
3. 处理音频轨道 (50%)
   - 裁剪音频片段
   - 应用音量调整
   - 处理静音
   ↓
4. 生成字幕 (60%)
   - 从文本轨道生成 SRT 文件
   - 转换为 ASS 格式（支持样式）
   - 设置字幕样式（背景、颜色、圆角）
   ↓
5. 合并视频轨道 (70%)
   - 使用 FFmpeg 拼接视频
   - 处理时间轴位置和间隙
   ↓
6. 合并音频轨道 (70%)
   - 混合多个音频片段
   - 处理音量和位置
   ↓
7. 合并视频和音频 (75%)
   ↓
8. 烧录字幕 (80%)
   - 将 ASS 字幕烧录到视频
   ↓
9. 上传到 US3 (90%)
   ↓
10. 更新数据库 (100%)
    - 保存导出记录到 creation.extra_data.outputs
    - 更新 creation.status 为 "completed"
```

**FFmpeg 命令示例**:

#### 视频轨道合并

```bash
# 裁剪单个视频片段
ffmpeg -i input.mp4 \
  -ss 2.5 -to 8.5 \
  -c copy output.mp4

# 拼接多个视频片段（带时间轴位置）
ffmpeg -i clip1.mp4 -i clip2.mp4 -i clip3.mp4 \
  -filter_complex "
    [0:v]trim=start=0:end=5,setpts=PTS-STARTPTS[v0];
    [1:v]trim=start=2:end=7,setpts=PTS-STARTPTS[v1];
    [2:v]trim=start=1:end=6,setpts=PTS-STARTPTS[v2];
    [v0][v1][v2]concat=n=3:v=1:a=0[vout]
  " \
  -map "[vout]" output.mp4
```

#### 音频轨道合并

```bash
# 混合多个音频（带音量调整）
ffmpeg -i audio1.mp3 -i audio2.mp3 -i audio3.mp3 \
  -filter_complex "
    [0:a]volume=1.0,adelay=0|0[a0];
    [1:a]volume=0.8,adelay=5000|5000[a1];
    [2:a]volume=1.2,adelay=10000|10000[a2];
    [a0][a1][a2]amix=inputs=3:duration=longest[aout]
  " \
  -map "[aout]" output.mp3
```

#### 字幕烧录

**ASS 字幕样式**:

```
[V4+ Styles]
Style: Default,Arial,48,&H00FFFFFF,&H000000FF,&H00000000,&HB0000000,-1,0,0,0,100,100,0,0,4,8,0,2,10,10,50,1

说明：
- Arial 字体，48px
- &H00FFFFFF: 白色文字
- &HB0000000: 半透明黑色背景（30% 不透明度）
- BorderStyle=4: 圆角背景框
- Outline=8: 圆角半径
- Alignment=2: 底部居中
- MarginV=50: 距离底部 50px
```

**烧录命令**:

```bash
ffmpeg -i video.mp4 \
  -vf "ass=subtitles.ass" \
  -c:v libx264 -preset medium -crf 23 \
  -c:a aac -b:a 192k \
  -movflags +faststart \
  output.mp4
```

**导出记录结构**:

```python
export_record = {
    "video_url": "https://us3.xxx.com/exports/uuid/timestamp.mp4",
    "export_at": "2024-01-06T10:30:00Z",
    "duration": 120.5,        # 视频时长（秒）
    "resolution": "1920x1080", # 分辨率
    "file_size": 52428800,    # 文件大小（字节）
    "status": "completed"      # 状态：completed/failed
}

# 存储位置
creation.extra_data['outputs'].append(export_record)
```

---

## 进度追踪系统

### 数据结构

所有任务的进度都存储在 `creation.extra_data.steps` 中：

```python
creation.extra_data = {
  "steps": {
    # 角色分析
    "characterAnalysis": {
      "status": "processing",  # processing/success/failed
      "triggered": True,
      "taskId": "celery-task-id",
      "updatedAt": 1704531000
    },

    # 场景解析
    "sceneAnalysis": {
      "status": "success",
      "triggered": True,
      "updatedAt": 1704531100
    },

    # 分镜解析
    "shotAnalysis": {
      "status": "success",
      "triggered": True,
      "updatedAt": 1704531200
    },

    # 批量视频生成
    "videoGeneration": {
      "status": "processing",
      "triggered": True,
      "taskId": "celery-task-id",
      "updatedAt": 1704531300,
      "progress": {
        "total": 50,         # 总共需要生成的视频数
        "completed": 25,     # 已完成的数量
        "success": 23,       # 成功的数量
        "failed": 2,         # 失败的数量
        "current_shot_id": 109  # 当前正在处理的分镜 ID
      }
    },

    # 视频导出
    "videoExport": {
      "status": "processing",
      "triggered": True,
      "taskId": "celery-task-id",
      "updatedAt": 1704531400,
      "progress": {
        "percent": 70,              # 百分比进度
        "status": "合并视频"         # 当前步骤描述
      },
      "result": {
        "video_url": "https://...",  # 导出完成后的视频 URL
        "file_size": 52428800
      }
    }
  },

  # 导出历史记录
  "outputs": [
    {
      "video_url": "https://us3.xxx.com/exports/uuid/1704531000.mp4",
      "export_at": "2024-01-06T10:30:00Z",
      "duration": 120.5,
      "resolution": "1920x1080",
      "file_size": 52428800,
      "status": "completed"
    }
  ]
}
```

### 前端轮询

```typescript
// 轮询查询创作进度
const { data: creation } = useQuery({
  queryKey: ["creation", creationId],
  queryFn: () => creationApi.queryCreationById(creationId),
  refetchInterval: 5000,  // 每 5 秒轮询一次
  enabled: isGenerating
});

// 读取进度
const videoGenProgress = creation?.extra_data?.steps?.videoGeneration?.progress;
const exportProgress = creation?.extra_data?.steps?.videoExport?.progress;
```

---

## 积分系统

### 积分消耗规则

```python
# 模型价格配置（app/utils/model_prices.py）
MODEL_PRICES = {
    # LLM 调用
    "gpt-4": {
        "input": 0.03,   # 每 1K tokens
        "output": 0.06
    },

    # 图片生成
    "stable-diffusion": {
        "512x512": 10,    # 每张图片
        "1024x1024": 20
    },

    # 视频生成
    "sora2": {
        "4s": 100,   # 4 秒视频
        "8s": 180,   # 8 秒视频
        "12s": 250   # 12 秒视频
    }
}
```

### 积分扣除流程

1. **冻结积分**: 任务开始前冻结所需积分
2. **执行任务**: 调用 AI 服务
3. **扣除或释放**:
   - 成功：扣除冻结的积分
   - 失败：释放冻结的积分

```python
# 示例：视频生成积分流程
def generate_video_with_points(shot_id, user_id):
    # 1. 计算所需积分
    duration = get_shot_duration(shot_id)
    required_points = calculate_video_cost("sora2", duration)

    # 2. 冻结积分
    freeze_record = PointsService.freeze_points(
        user_id=user_id,
        points=required_points,
        operation_type="generate_video"
    )

    try:
        # 3. 执行视频生成
        video_url = generate_video(shot_id)

        # 4. 成功：扣除积分
        PointsService.deduct_frozen_points(
            freeze_record_id=freeze_record.record_id
        )

        return video_url

    except Exception as e:
        # 5. 失败：释放积分
        PointsService.release_frozen_points(
            freeze_record_id=freeze_record.record_id,
            reason=f"视频生成失败: {str(e)}"
        )
        raise
```

---

## 使用指南

### 1. 创建新作品

```
1. 上传小说文本（txt 格式）
   ↓
2. 系统自动解析章节
   ↓
3. 选择章节，点击"开始创作"
   ↓
4. 进入动态漫编辑器
```

### 2. 生成流程

```
步骤 1: 分析角色和场景
- 点击"角色"标签 → "分析角色"
- 等待 LLM 分析完成
- 点击"生成全部角色图片"

步骤 2: 生成场景
- 点击"场景"标签 → "解析场景"
- 等待分析完成
- 点击"生成全部场景图"

步骤 3: 生成分镜
- 点击"分镜"标签 → "解析分镜"
- 等待分镜拆分完成
- 点击"生成全部分镜图"

步骤 4: 生成视频
- 点击"生成全部视频"
- 系统会逐个生成视频（可能需要较长时间）
- 可在分镜详情中查看每个视频的生成进度

步骤 5: 导入轨道
- 点击"一键导入素材"
- 所有视频会自动添加到时间轴

步骤 6: 编辑
- 调整视频片段的位置和时长
- 添加音频和字幕
- 实时预览效果

步骤 7: 导出视频
- 点击"导出视频"
- 等待合并完成（通常几分钟）
- 在"导出历史"中查看结果
```

### 3. 查看导出历史

```
1. 点击"导出视频"按钮
   ↓
2. 点击"导出历史"标签
   ↓
3. 查看所有导出记录
   ↓
4. 点击"预览"在线观看
   或
   点击"下载"保存到本地
```

---

## 性能优化

### 1. 异步处理

所有耗时任务都使用 Celery 异步处理：
- 角色分析
- 场景解析
- 图片生成
- 视频生成
- 视频导出

### 2. 批量处理优化

**批量视频生成**:
- 顺序处理（避免并发冲突）
- 单个失败不影响其他
- 实时进度更新
- 积分不足自动停止

### 3. 缓存策略

- 生成的图片和视频永久存储在 US3
- 前端使用 React Query 缓存 API 响应
- 时间轴配置保存在数据库，支持断点续传

### 4. 超时处理

- 单个视频生成: 30 分钟超时
- 批量视频生成: 2 小时超时
- 视频导出: 默认无超时（通常 5-10 分钟）

### 5. 错误重试

- API 调用失败: 自动重试 3 次（502/503/504 错误）
- 指数退避: 2s → 4s → 8s
- 下载失败: 重试 3 次

---

## 常见问题

### Q1: 视频生成需要多长时间？

**A**: 取决于分镜数量：
- 单个视频: 5-15 分钟
- 批量生成: 每个视频 5-15 分钟，顺序处理
- 50 个分镜约需 4-12 小时

### Q2: 导出的视频质量如何？

**A**:
- 分辨率: 1920x1080 (Full HD)
- 视频编码: H.264
- 音频编码: AAC 192kbps
- 帧率: 30fps
- 字幕: 烧录到视频中

### Q3: 如何节省积分？

**A**:
- 减少不必要的重新生成
- 使用较短的视频时长（4s 比 12s 便宜）
- 复用已生成的素材

### Q4: 批量生成失败了怎么办？

**A**:
- 检查积分是否充足
- 查看失败的分镜，单独重试
- 失败的分镜不会扣除积分

### Q5: 可以修改已生成的内容吗？

**A**:
- 角色、场景、分镜的文字描述可以编辑
- 编辑后点击"重新生成"即可
- 时间轴中的素材可以自由编辑

---

## 技术亮点

1. **完整的 AI 驱动流程**: 从文本到视频全自动
2. **灵活的提示词工程**: 每个环节都有精心设计的提示词
3. **可视化时间轴编辑**: 像 Premiere Pro 一样编辑视频
4. **实时进度追踪**: JSONB 存储，前端轮询显示
5. **积分系统**: 冻结-扣除-释放机制，确保公平
6. **容错机制**: 单点失败不影响整体，支持重试
7. **导出历史**: 记录所有版本，随时回溯

---

## 未来优化方向

1. **并行生成**: 部分环节支持并发处理
2. **增量导出**: 只导出修改的部分
3. **云端渲染**: 使用分布式渲染加速
4. **AI 优化**: 更智能的分镜拆分和镜头设计
5. **模板系统**: 预设风格和效果模板
6. **实时协作**: 多人同时编辑

---

## 附录

### A. 关键文件清单

**后端任务**:
- `app/tasks/creation_task.py` - 角色分析
- `app/tasks/scene_task.py` - 场景解析和图片生成
- `app/tasks/shot_task.py` - 分镜拆分和图片生成
- `app/tasks/step7_video_prompt_gen_task.py` - 视频提示词生成
- `app/tasks/step8_video_gen_task.py` - 视频生成
- `app/tasks/video_export.py` - 视频导出

**API 端点**:
- `app/api/api_v1/endpoints/creations.py` - 创作相关 API

**工具类**:
- `app/utils/ai_client.py` - AI 服务调用
- `app/utils/ffmpeg_utils.py` - FFmpeg 视频处理
- `app/utils/us3.py` - US3 对象存储

**前端组件**:
- `src/components/business/timeline/` - 时间轴编辑器
- `src/components/business/video-preview/` - 视频预览
- `src/components/timeline/export-dialog.tsx` - 导出对话框
- `src/app/[locale]/dynamic-comic-editor/page.tsx` - 主编辑器页面

### B. 数据库表结构

```sql
-- 创作表
CREATE TABLE creation (
    creation_id SERIAL PRIMARY KEY,
    uuid UUID UNIQUE,
    title VARCHAR(255),
    status VARCHAR(50),
    owner_id INTEGER,
    novel_id INTEGER,
    chapter_id INTEGER,
    timeline_config JSONB,  -- 时间轴配置
    extra_data JSONB,       -- 进度和导出记录
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- 角色表
CREATE TABLE character (
    character_id SERIAL PRIMARY KEY,
    name VARCHAR(255),
    appearance TEXT,
    body_features TEXT,
    hair TEXT,
    clothing TEXT,
    feature_tags TEXT,
    image_url TEXT,         -- 角色形象 URL
    creation_id INTEGER
);

-- 场景表
CREATE TABLE scene (
    scene_id SERIAL PRIMARY KEY,
    scene_number INTEGER,
    title VARCHAR(255),
    time VARCHAR(255),
    location VARCHAR(255),
    atmosphere TEXT,
    image_url TEXT,         -- 场景图片 URL
    creation_id INTEGER
);

-- 分镜表
CREATE TABLE shot (
    shot_id SERIAL PRIMARY KEY,
    shot_number INTEGER,
    description TEXT,       -- 画面描述
    narration TEXT,         -- 旁白
    dialogue TEXT,          -- 台词
    image_url TEXT,         -- 分镜图片 URL
    video_url TEXT,         -- 分镜视频 URL
    video_status VARCHAR(50),
    status_detail JSONB,    -- 视频生成状态详情
    extra_data JSONB,       -- video_prompt 等
    scene_id INTEGER
);
```

---

**文档版本**: v1.0
**最后更新**: 2024-01-06
**维护者**: Mira Team
